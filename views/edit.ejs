<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note - Notepad App</title> <!-- Changed title for clarity -->
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
        <!-- Go back link -->
        <a class="text-blue-500 inline-block mb-10 hover:underline" href="/">← Go back to home</a> <!-- Added hover effect -->

        <!-- Edit form -->
        <form action="/edit" method="post" class="max-w-lg">
            <!-- Previous filename (read-only) -->
            <label class="block mb-2 text-zinc-400">Current Note Name</label>
            <input class="block w-full text-zinc-200 rounded-md outline-none ps-5 py-2 mb-4 bg-zinc-800"
                type="text" name="previous" value="<%= filename %>" readonly> <!-- changed text color to make visible -->

            <!-- New filename -->
            <label class="block mb-2 text-zinc-400">New Note Name</label>
            <input class="block w-full rounded-md outline-none ps-5 py-2 mb-4 bg-zinc-800"
                type="text" name="new" placeholder="Enter new name">

            <!-- Submit button -->
            <input class="bg-yellow-600 hover:bg-yellow-500 transition-colors px-5 py-2 rounded-md cursor-pointer"
                type="submit" value="Update Note">
        </form>
    </div>
</body>

</html>
