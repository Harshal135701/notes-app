<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit - <%= title.replace('.txt','') %> | My Notes</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-purple-900 via-pink-900 to-gray-900 text-gray-100 font-sans min-h-screen flex items-center justify-center p-6">

  <div class="w-full max-w-3xl bg-gray-800 rounded-2xl shadow-2xl p-8">

    <!-- Header -->
    <h1 class="text-3xl font-extrabold mb-6 text-gradient bg-clip-text text-transparent bg-gradient-to-r from-yellow-400 to-red-400">
      ✏️ Edit Note: <%= title.replace('.txt','') %>
    </h1>

    <!-- Edit Form -->
    <form action="/edit/<%= encodeURIComponent(title) %>" method="post" class="space-y-4">
      <textarea 
        name="details"
        class="w-full h-56 p-5 bg-gray-700 border border-gray-600 rounded-xl focus:outline-none focus:ring-2 focus:ring-yellow-400 text-gray-100 text-lg leading-relaxed placeholder-gray-400"
        required
      ><%= content %></textarea>

      <button 
        type="submit"
        class="w-full bg-yellow-500 hover:bg-yellow-600 text-gray-900 font-bold py-3 rounded-xl shadow-lg hover:shadow-2xl transition-all duration-200"
      >
        💾 Save Changes
      </button>
    </form>

    <!-- Back Button -->
    <a href="/" 
       class="inline-block mt-6 px-6 py-2 bg-blue-500 hover:bg-blue-600 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-200 text-white">
       ← Back to Notes
    </a>

  </div>

</body>
</html>
